import React from "react";

const JsxRules = () => {
  return (
    <div>
      <h2>JSX Rules</h2>

      <p>1. JSX must have a single parent element.</p>
      <p>2. JSX elements must be properly closed.</p>
      <p>3. JSX attributes are written in camelCase.</p>
      <p>4. Example: className instead of class.</p>
    </div>
  );
};

export default JsxRules;
